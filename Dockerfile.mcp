FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package.json package-lock.json* ./
RUN npm ci

# Copy source files
COPY . .

# MCP server runs via tsx (TypeScript execution)
CMD ["npx", "tsx", "src/mcp/server.ts"]
